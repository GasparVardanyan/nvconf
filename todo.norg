* Todo List (v1)

  - (=) nvim-cmp.lua - move mappings to *mappings* module
  - (x) nvim-cmp.lua - move lsp setups to *nvim-lspconfig.lua*
  - (=) dap integration
  - (=) cmake integration
  - (=) clang integration
  - (x) compiler.nvim integration
  - (x) separate plugin configs
  - ( ) overseer lualine

* Todo List (v2)
  ~ ( ) *Module* core
  ~~ (x) Make plugin.priority as parameter and set to all plugins
  ~~ (x) Take single argument: a table with name, plugins, autocmds, etc..
  ~~ (=) Make module dependencies so one module can wait other to be loaded
  ~~ (x) Module specific autocommands and mappings
  ~~ (x) Separate plugin specs from modules
  ~ ( ) *Modules*
  ~~ (x) Separate base logic
  ~~ (x) HlsModule
  ~~ (!) LspModule
  ~~~ (-) refactor nvim-lspconfig spec
  ~~~ (-) refactor nvim-cmp spec
  ~~~ (-) refactor LuaSnip spec
  ~~ (-) BasicModule
  ~~ (-) CompilerModule
  ~~ (-) DapModule
  ~~~ ( ) https://github.com/nvim-telescope/telescope-dap.nvim
  ~~ (x) UIModule
  ~~ (x) DbModule
  ~~ (=) LintModule
  ~~~ ( ) https://github.com/mfussenegger/nvim-lint
  ~~ (=) AiModule
  ~~~ ( ) https://github.com/yetone/avante.nvim
  ~~ (=) ClangModule
  ~~~ ( ) https://sr.ht/~p00f/clangd_extensions.nvim
  ~~~ ( ) https://github.com/madskjeldgaard/cppman.nvim
  ~~ (=) NoneLSModule
  ~~~ ( ) https://github.com/nvimtools/none-ls.nvim
  ~~ (=) TestModule
  ~~~ ( ) https://github.com/nvim-neotest/neotest
  ~~~ ( ) https://github.com/alfaix/neotest-gtest
  ~~ (x) GitModule
  ~~ (x) NvimModule (/lazydev.nvim/, /nvim-nio/)
  ~~ (x) OrgModule
  ~~ (=) MdModule
  ~~ ( ) Move qmake utils to *modular.extras*
  ~ (x) ModuleManager
  ~ (_) MappingManager
  ~ (x) Migrate Options
  ~ (x) Migrate Autocmds
  ~ (x) Replace nvim-tree with neo-tree
  ~ (x) Commenting /nerdcommenter/
  ~ *ModuleManager*
  ~~ ( ) Handle duplicate specs
  ~~ (x) Handle loaded plugin registration here not in *Module*, so modules can
		 depend on plugins from other modules /make a function which registers/
		 /and loads the spec/
  ~ (x) Dashboard
  ~ ( ) Module leaders /whichkey?/
  ~ ( ) Workspace Manager /https://github.com/natecraddock/workspaces.nvim/
  ~ ( ) Persistence /https://github.com/folke/persistence.nvim/
  ~ ( ) Restructure
  ~~ (x) Move included Modules outside the *ModuleManager*
  ~~ (x) New directory structure to have multiple module groups
  ~~ (_) Add *kickstart-modular* as a separate *Module*\/*Modules*

** ChatGPT
   - (x) Instead of one autocmd per module, consider a single User ModuleReady event with a data field to indicate which module is ready.
